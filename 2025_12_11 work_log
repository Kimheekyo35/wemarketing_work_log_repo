# 2025_12_11 work_log

🔥 금일 목표
 - 재무팀 엑셀 파일들을 하나로 병합해주는 파일 생성

🚦 업무 순서
 - 엑셀 파일에서 9번째 행을 column명으로 두기
 - 엑셀 파일명에서 Brand, Country를 갖고와 새로운 기존 데이터에 새로운 두 개의 열 추가하기

🚨 문제점
 - to_excel 시간 너무 많이 걸림 (10분 이상 소요) → to_csv 로 변환하니까 시간이 적게 걸림 (재무팀에서 csv파일을 받을 진 모르겠지만..)
 - csv로 열 때 한글, 중국어, 숫자 깨짐 발생 → 저장할 때 encoding=’utf-8’ 로 함

💫✏️📌
 - 엑셀을 열고 특정 sheet를 지정하면 sheet에 포함된 데이터를 가져올 수 있음
   origin_col = pd.read_excel(file,sheet_name=sheet_name, header=9).columns.tolist()

 - 데이터가 0부터 시작되는 게 아니니까 어디서부터 데이터가 시작되는지 파악할 수 있어야 함 -> skiprows로 갖고오고 싶은 행의 시작점 정할 수 있음.
   skiprows는 0-11번째 행 날리고 12번째 행부터 데이터로 인식

 - 파일명에서 국가와 브랜드 가져오는 방법
  file = AMAZON_FWEE_US
  stem_parts = file.stem.split("_")

  brand = stem_parts[1]
  country = stem_parts[2]

- 열 위치 바꿔주는 방법 (insert 로 해결 가능)
 # Brand 열을 0번째로 이동시키고 기존에 있던 Brand열을 pop 시키기 (제거)
  df.insert(0,"Brand",df.pop('Brand'))
  
