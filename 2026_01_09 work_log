# 2026_01_09 work_log

🔥 금일 목표
1. 로그인이 필요없는 부분도 proxy 오류 발생 해결
2. playwright으로 로그인하여 쿠키 갱신해오기

🚦 업무 순서
1. TIMEOUT 시간 느리기 (proxy)
2. playwright으로 로그인 완료하기
  - storage_state.json 재저장 (자동으로 갱신)
  - 이 json 사용해서 로그인 

🚨 문제점
1. 내가 로컬에서 로그인한 쿠키와 proxy를 이용해서 로그인한 쿠키의 값이 다름 -> playwright으로 로그인하여 쿠키 갱신
2. X에서 로그인 과정을 막아서 못 들어감

💫✏️📌
10000ms -> 1초.
3000초로 설정하였더니 로딩이 하기 전에 다른 페이지로 넘어가서 요소들을 못 가져오는 오류가 발생함.
30000초로 설정하여 오류 해결함.

- X 페이지에서 html tag가 4개 존재했음. 그 중에서 text가 'Log in'인 부분을 가져오고 싶었음
처음엔 .text를 쓰려고 했지만 page.get_by_role("button").filter(has_text="Log in").click() 라는 코드를 배웠음.
  => html tag가 button이고 text가 log in인 버튼을 클릭하게 하는 코드

- 또한 X 페이지에서 로그인 버튼을 누른 후에 오류가 뜨는 게 속도가 너무 빠른 점이 봇으로 탐지하게 했다고 판단
  => 타이핑을 치는 느낌을 주는 코드 발견. 여기서 type은 typing이고 delay는 타자치는 속도를 의미함 120 ~ 150이 적당함
  => page.locator("input[name='text']").type(ID,delay=150)

- get_by_role 은 "접근성 역할"을 기준으로 요소를 찾는 것
|------HTML-----------|-----접근성role-----.|
|    <button>         |       button       |
|    <a href =" "     |       link         |
| input type="check"  |      checkbox      |
|      <h1>           |      heading       |
|      <ul>           |        list        |
|      <li>           |       listitem     |
|     <table>         |       table        |
|------------------------------------------|
-https://playwright.dev/docs/locators 참고 -
