# 2026_01_07 work_log

🔥 금일 목표
1. X 크롤링 서버에 옮겨서 테스트 (로그아웃 크롤러 + 쿠키 크롤러)
2. 만약 제대로 크롤링 되지 않을 시 이메일이 오도록 설정

🚦 업무 순서
1. 로그인이 필요한 url + 필요없는 url로 테스트 시트를 만들어 로컬에서 실행시키기
2. 제대로 실행됨 확인 후, 서버로 옮기기
3. 접속은 되지만 필요한 value가 모두 0일 경우가 50개가 넘을 경우는 아이디가 차단당했다고 판단하여 이메일이 오도록 설정

🚨 문제점
1. 이메일 코드에서 제목도 설정하지 않고, 보내는 코드가 없어서 이메일이 계속 보내지지 않는 문제 발생
2. load_dotenv() 를 하지 않아 .env 파일에 있는 정보를 갖고오지 못함.

💫✏️📌
- python에서 group() 함수
   m = re.match('([0-9]+) ([0-9]+)', '10 295') 일 떄
   m.group() = 10 295
   m.group(1) = 10
   m.group(2) = 295 임.

- python에서 join() 함수
a = ['BlockDMask', 'python', 'join', 'example']
result1 = ", ".join(a) -> ,로 a를 join 시키라는 뜻
result1 = BlockDMask, python, join, example 가 됨.

- log 설정 바꾸기
logging.basicConfig(
   filename = : ,
   filemode = 'a',
   format = :,
   datefmt = :,
    encoding = 'utf-8',
   # DEBUG 이상되는 건 log에 저장하겠다는 의미
    level = logging.DEBUG
)
# log_x_crawling_test 파일에 log를 저장하겠다는 의미
logger = logging.getLogger("log_x_crawling_test")

- 이메일 자동화
SMTP 란? 아웃룩 같은 이메일을 보내주는 서버임.
이메일을 보내기 위해선 계정을 생성해주고 시트에 client_email 주소를 입력해주고 권한 부여해야 함.

# 암호화 통신
암호화 방식에 따라 TLS, SSL 로 나뉨.
- TLS: smtplib.SMTP("서버명", "포트번호)
- SSL: smtplib.SMTP_SSL("서버명", "포트번호) 사용함

# 본문 + 헤더를 포함한 메일 구성 단계
 msg = MIMEMultipart()
 msg["Subject"] = f"X_crawling 오류관련 메일"
 body = f"{error_reason}\n X URL 접근 실패"
 msg.attach(MIMEText(body, "plain", _charset="utf-8"))
 msg["From"] = sender_email
 msg["To"] = "diana0305@wemarketing.co.kr"

# SMTP 서버에 보내는 코드
 with smtplib.SMTP("smtp.gmail.com",587) as connection:
     # connection.set_debuglevel(1) # 로그 확인용
     connection.starttls()
     connection.login(user=sender_email , password=sender_password)
     # 이 코드가 있어야 보내짐
     connection.sendmail(
         from_addr = sender_email,
         to_addrs= ["###"],
         msg = msg.as_string()
         )


